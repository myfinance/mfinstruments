server.port: 7001
logging:
  level:
    root: INFO
    de.hf.myfinance: DEBUG

spring.jpa.hibernate.ddl-auto: none
spring.datasource:
  url: jdbc:postgresql://192.168.100.73:30020/marketdata
  username: postgres
  password: vulkan

springdoc:
  swagger-ui.path: /openapi/swagger-ui.html
  api-docs.path: /openapi/v3/api-docs
  packagesToScan: de.hf.myfinance
  pathsToMatch: /**

api:

  common:
    version: 1.0.0
    title: MF instruments API
    description: REST API to create update and read instruments.

  responseCodes:
    ok.description: OK
    badRequest.description: Bad Request, invalid format of the request. See response message for more information
    notFound.description: Not found, the specified id does not exist
    unprocessableEntity.description: Unprocessable entity, input parameters caused the processing to fail. See response message for more information

---
spring.config.activate.on-profile: docker

spring.datasource:
  url: jdbc:postgresql://mfdb:30020/marketdata
  username: postgres
  password: vulkan