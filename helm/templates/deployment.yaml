apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.mfinstruments.mf_name }} 
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.mfinstruments.mf_name }}    
  template:
    metadata:
      labels:
        app: {{ .Values.mfinstruments.mf_name }}    
    spec:
      containers:
        - name: {{ .Values.mfinstruments.mf_name }}      
          image: "{{ .Values.image.repository }}mfinstruments:{{ .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}      
          tty: true     
          volumeMounts:
           - name: mfinstrumentsconfig
             mountPath: /MyFinance/envconfig           
      volumes:
        - name: mfinstrumentsconfig
          persistentVolumeClaim:
              claimName: {{ .Values.stage }}-{{ .Values.pv.configpvc }}                                    
